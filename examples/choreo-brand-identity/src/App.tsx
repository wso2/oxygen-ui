import React, {ReactElement} from 'react';
import './App.css';
import LoginPage from './pages/LoginPage';

const App = (): ReactElement => (
  <div className="App">
    <LoginPage />
  </div>
);

export default App;
