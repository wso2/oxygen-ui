---
'@wso2/oxygen-ui': minor
---

Publish real Material-shaped subpath entries (e.g. `import Button from '@wso2/oxygen-ui/Button'`) so path imports resolve without `@wso2/vite-plugin-oxygen-ui`. The barrel `import { Button } from '@wso2/oxygen-ui'` is unchanged.
