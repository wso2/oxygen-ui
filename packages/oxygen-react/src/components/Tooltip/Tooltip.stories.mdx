import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import IconButton from '@mui/material/IconButton';
import StorybookHierarchy from '../../storybook-helpers/hierarchy.ts';
import Tooltip from './Tooltip.tsx';

export const meta = {
  component: Tooltip,
  title: StorybookHierarchy.Tooltip,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => (
  <Tooltip title="Light" {...args}>
    <IconButton />
  </Tooltip>
);

# Tooltip

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

Tooltips display informative text when users hover over, focus on, or tap an element.

<Canvas>
  <Story name="Overview" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `Tooltip` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import Link from '@oxygen/react/Tooltip';\n
function Demo() {
  return (
    <Tooltip title="I'm a tooltip">
      { /* Wrapped element */ }
    </Tooltip>
  );
}`}
/>

## Variants

### Basic

A basic tooltip variant.

<Canvas>
  <Story name="Basic" args={{title: 'Light'}} />
</Canvas>
