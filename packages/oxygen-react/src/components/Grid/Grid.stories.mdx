import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import Paper from '@mui/material/Paper';
import StorybookHierarchy from '../../storybook-helpers/hierarchy.ts';
import Grid from './Grid.tsx';

export const meta = {
  component: Grid,
  title: StorybookHierarchy.Grid,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => (
  <Grid container spacing={2} {...args}>
    <Grid item xs={8}>
      <Paper sx={{padding: 1, textAlign: 'center'}}>xs=8</Paper>
    </Grid>
    <Grid item xs={4}>
      <Paper sx={{padding: 1, textAlign: 'center'}}>xs=4</Paper>
    </Grid>
    <Grid item xs={4}>
      <Paper sx={{padding: 1, textAlign: 'center'}}>xs=4</Paper>
    </Grid>
    <Grid item xs={8}>
      <Paper sx={{padding: 1, textAlign: 'center'}}>xs=8</Paper>
    </Grid>
  </Grid>
);

# Grid

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

The `grid` creates visual consistency between layouts while allowing flexibility across a wide variety of designs. It is
based on a `12-column` grid layout.

<Canvas>
  <Story name="Overview" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `Button` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import Grid from '@oxygen-ui/react/Grid';\n
function Demo() {
  return <Grid>{ /* Content */ }</Grid>;
}`}
/>

## Variants

### Basic

Variation that provides an extra visual weight to identify the primary action from a set of buttons.

<Canvas>
  <Story name="Basic" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
