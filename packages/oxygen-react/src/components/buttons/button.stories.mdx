import { ComponentStory, ComponentMeta } from '@storybook/react';
import { STORYBOOK_HIERARCHY } from '../../storybook-helpers';
import { PrimaryButton, PrimaryButtonProps } from './primary-button';
import { Button, ButtonProps } from "./button";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";

export const meta = {
  component: PrimaryButton,
  title: STORYBOOK_HIERARCHY.BUTTON,
};

<Meta
  title={ meta.title }
  component={ meta.component }
/>

<!--- Component template -->

export const Template = (args) => <PrimaryButton {...args} />;

<!--- Component documentation -->

# Button
- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

Buttons allow users to trigger an action or event with a single click.
For example, you can use a button for allowing the functionality of submitting a form, opening a dialog, canceling an action,
or performing a delete operation.

<Canvas>
  <Story name="Overview"
         args={{children: "Button"}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props
<ArgsTable story="Overview" />

## Variants

### Primary

This variant could be used for actions that requires user's attention right away or to give more presidence to an action if there are other actions available in the view.

<Canvas>
  <Story name="Primary">
    <PrimaryButton>Primary</PrimaryButton>
  </Story>
</Canvas>
