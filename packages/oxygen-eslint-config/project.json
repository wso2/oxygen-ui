{
  "name": "oxygen-eslint-config",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/oxygen-eslint-config",
  "projectType": "library",
  "targets": {
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs oxygen-eslint-config {args.ver} {args.tag}"
      },
      "dependsOn": ["build"]
    }
  },
  "tags": ["scope:oxygen-eslint-config"]
}
