import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import CountryFlag from './CountryFlag';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';

export const meta = {
  component: CountryFlag,
  title: StoryConfig.CountryFlag.hierarchy
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <CountryFlag {...args} />;

# CountryFlag

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)

## Overview

CountryFlag can be used to show the flag of the country for the provided country code.

<Canvas>
  <Story name="Overview" args={{
    title: "Sri Lanka",
    countryCode: "LK"
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props
<ArgsTable story="Overview" />

## Usage

Import and use the `CountryFlag` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import CountryFlag from '@oxygen-ui/react/CountryFlag';\n
function Demo() {
  return (
    <CountryFlag
      title="Sri Lanka"
      countryCode="LK"
    />
  );
}`}
/>
