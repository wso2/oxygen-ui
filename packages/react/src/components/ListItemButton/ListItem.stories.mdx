import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import {withDesign} from '../../../.storybook/utils.ts';
import ListItemButton from './ListItemButton.tsx';

export const meta = {
  component: ListItemButton,
  title: StoryConfig.ListItemButton.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <ListItemButton {...args} />;

# ListItemButton

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)

## Overview

`ListItemButton` is a list item that makes it functional or clickable.

<Canvas>
  <Story name="Overview" args={{children: 'List Item Button'}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `ListItemButton` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import ListItemButton from '@oxygen-ui/react/ListItemButton';\n
function Demo() {
  return (
    <ListItemButton>
      {/* List Item Button Content */}
    </ListItemButton>
  );
}`}
/>
