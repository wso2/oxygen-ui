import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import CollapsibleNavbarItem from './CollapsibleNavbarItem.tsx';

export const meta = {
  component: CollapsibleNavbarItem,
  title: StoryConfig.CollapsibleNavbarItem.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <CollapsibleNavbarItem {...args} />;

# CollapsibleNavbarItem

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)

## Overview

`CollapsibleNavbarItem` is an item with expand/collapse functionality that can display sub-items.

<Canvas>
  <Story name="Overview" args={{label: 'Collapsible Item', items: [{label: "Item"}]}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `CollapsibleNavbarItem` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import CollapsibleNavbarItem from '@oxygen-ui/react/CollapsibleNavbarItem';\n
function Demo() {
  return (
    <CollapsibleNavbarItem label="Collapsible Item" items=[{label: "Item"}]></CollapsibleNavbarItem>
  );
}`}
/>
