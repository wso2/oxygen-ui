import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import Snackbar from '../Snackbar';

export const meta = {
  component: Snackbar,
  title: StoryConfig.Snackbar.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <Snackbar {...args} />;

# Snackbar

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)

## Overview

The Snackbar can be used to display notifications.

<Canvas>
  <Story
    name="Overview"
    args={{message: 'This is a sample message!', open: true}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `Snackbar` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`import Snackbar from '@oxygen-ui/react/Snackbar';\n`}
/>
