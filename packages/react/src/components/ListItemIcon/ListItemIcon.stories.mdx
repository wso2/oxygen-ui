import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import {LockIcon} from '@oxygen-ui-experimental/react-icons';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import {withDesign} from '../../../.storybook/utils.ts';
import ListItemIcon from './ListItemIcon.tsx';

export const meta = {
  component: ListItemIcon,
  title: StoryConfig.ListItemIcon.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <ListItemIcon {...args} />;

# ListItemIcon

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)

## Overview

A simple wrapper to apply List styles to an Icon or SvgIcon .

<Canvas>
  <Story name="Overview" args={{children: <LockIcon />}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `ListItemIcon` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import ListItemIcon from '@oxygen-ui-experimental/react/ListItemIcon';\n
function Demo() {
  return (
    <ListItemIcon>
      {/* Icon */}
    </ListItemIcon>
  );
}`}
/>
