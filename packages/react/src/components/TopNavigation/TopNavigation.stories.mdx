import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import {withDesign} from '../../../.storybook/utils.ts';
import TopNavigation from './TopNavigation.tsx';
import {LanguagesIcon, LogsIcon} from '@oxygen-ui/react-icons';

export const meta = {
  component: TopNavigation,
  title: StoryConfig.TopNavigation.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <TopNavigation {...args} />;

# Top Nav

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

The top Navigation bar provides content and actions related to the current screen. It's used for branding, screen
titles, navigation, and actions.

<Canvas>
  <Story
    name="Overview"
    parameters={withDesign('TopNavigation', 'Overview', {})}
    args={{
      isLeftNavigationActive: true,
      brand: {
        logo: {
          desktop: <img src='/assets/images/oxygen-ui-logo-mini.svg' alt='logo' />,
          mobile: <img src='/assets/images/oxygen-ui-logo-mini.svg' alt='logo' />,
          },
        onClick: () => null,
        title: 'OxygenUI',
      },
      user: {
        image: '/assets/images/avatar-john.svg',
        name: 'Matthew',
        email: 'matthew@wso2.com',
      },
      //TODO: Add Storybook action.
      onLogOut: () => null,
      links: [
        {
          children: 'Console', 
          startIcon: <LogsIcon />, 
          href: '/'
          },
        {
          children: 'Language', 
          startIcon: <LanguagesIcon />, 
          href: '/'
          },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />
