import {ConsoleUserThickIcon, HomeFilledIcon, LockIcon, PreferencesIcon, LanguagesIcon, SunIcon} from '@oxygen-ui/react-icons';
import {useArgs} from '@storybook/client-api';
import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import Navbar from './Navbar.tsx';

export const meta = {
  component: Navbar,
  title: StoryConfig.Navbar.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const navbarItems = [
  {
    items: [{
      icon: <HomeFilledIcon />,
      name: 'Overview',
      selected: true,
    },
    {
      icon: <ConsoleUserThickIcon />,
      name: 'Profile Info',
    },
    {
      icon: <LockIcon />,
      name: 'Security',
    }],
  },
  {
    items: [{
      icon: <LanguagesIcon />,
      name: 'Language',
    },
    {
      icon: <PreferencesIcon />,
      name: 'Preferences',
    }],
  },
];

export const navbarItemsHeading = [
  {
    heading: 'Account',
    items: [{
      icon: <HomeFilledIcon />,
      name: 'Overview',
      selected: true,
    },
    {
      icon: <ConsoleUserThickIcon />,
      name: 'Profile Info',
    },
    {
      icon: <LockIcon />,
      name: 'Security',
    }],
  },
  {
    heading: 'Settings',
    items: [{
      icon: <LanguagesIcon />,
      name: 'Language',
    },
    {
      icon: <PreferencesIcon />,
      name: 'Preferences',
    }],
  },
    {
    heading: 'Theming',
    items: [{
      icon: <SunIcon />,
      name: 'Customize',
    },
    ],
  },
];

export const Template = args => {
  const [runtimeArgs, updateArgs] = useArgs();
  return (
    <Navbar
      onClose={() => updateArgs({...runtimeArgs, open: false})}
      onOpen={() => updateArgs({...runtimeArgs, open: true})}
      {...args}
    />
  );
};

# Navbar

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

Use the `Navbar` component to display a list of items in a sidebar.

<Canvas>
  <Story
    name="Overview"
    parameters={{
      docs: {inlineStories: false, iframeHeight: 476},
    }}
    args={{items: navbarItems}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `Navbar` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import Navbar from '@oxygen-ui/react/Navbar';\n
function Demo() {
  return (
    <Navbar items={navbarItems} />
  );
}`}
/>

## Variants

### Headings

Navbar with headings.

<Canvas>
  <Story
    name="Headings"
    args={{items: navbarItemsHeading, open: true}}
    parameters={{
      docs: {inlineStories: false, iframeHeight: 476},
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Gradient

Navbar with a gradient as the background color.

<Canvas>
  <Story
    name="Gradient"
    args={{fill: 'gradient', items: navbarItems}}
    parameters={{
      docs: {inlineStories: false, iframeHeight: 476},
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
