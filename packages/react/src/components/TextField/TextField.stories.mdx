import {ArgsTable, Source, Story, Canvas, Meta} from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import TextField from './TextField.tsx';

export const meta = {
  component: TextField,
  title: StoryConfig.TextField.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => <TextField {...args} />;

# Text Field

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

Elements that allows users to edit text on interfaces.

<Canvas>
  <Story name="Overview" args={{label: 'Username', placeholder: 'Enter your username'}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

Import and use the `TextField` component in your components as follows.

<Source
  language="jsx"
  dark
  format
  code={dedent`
import TextField from '@oxygen-ui/react/TextField';\n
function Demo() {
  return <TextField />;
}`}
/>

## Variants

### Basic

A component that wraps an input filed and an input label.

<Canvas>
  <Story name="Basic" args={{label: 'Username', placeholder: 'Enter your username'}} />
</Canvas>
