import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import StoryConfig from '../../../.storybook/story-config.ts';
import DateTimePicker from './DateTimePicker.tsx';
import LocalizationProvider from '../LocalizationProvider/LocalizationProvider.tsx';
import { AdapterMoment } from '@mui/x-date-pickers/AdapterMoment';

export const meta = {
  component: DateTimePicker,
  title: StoryConfig.DateTimePicker.hierarchy,
};

<Meta title={meta.title} component={meta.component} />

export const Template = args => (
  <LocalizationProvider dateAdapter={AdapterMoment}>
    <DateTimePicker {...args} />
  </LocalizationProvider>
);

# DateTimePicker

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)

## Overview

Use the DateTimePicker component to let users pick a date and time together, with responsive adaptation between mobile and desktop views.

<Canvas>
  <Story
    name="Overview"
    args={{
      label: 'Date & Time',
      disabled: false,
      disableFuture: false,
      disableHighlightToday: false,
      disablePast: false,
      loading: false
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

<Source
  language="jsx"
  dark
  format
  code={dedent`
import DateTimePicker from '@oxygen-ui/react/DateTimePicker';\n
function Demo() {
  return (
    <DateTimePicker label="Date & Time" />
  );
}
`}
/>
